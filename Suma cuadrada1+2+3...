#include "stdafx.h"
#include <iostream>
#include "conio.h"
#include "math.h"
using namespace std;
void main()
{float i;
	int n,suma;
	cout<<"Hasta que nurmero quiere sumar: ";
	cin>>n;
	suma=0;
	i=1;
	while(i<=n)
	{
		suma=suma+pow(i,2);
		i=i+1;
	}
	cout<<"La suma es: "<<suma;
	getch();
}
